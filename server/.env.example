# Copy to .env — or better, run `python3 ../tools/stack.py init` which
# generates this with a random password AND creates mosquitto/passwd to match.
MQTT_USER=printtrek
MQTT_PASSWORD=change-me
TZ=Europe/Stockholm
